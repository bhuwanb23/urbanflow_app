{
  "name": "urbanflow-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "UrbanFlow monorepo root — workspace dev tooling only",
  "scripts": {
    "validate:compose": "node scripts/validate-compose.js"
  },
  "devDependencies": {
    "js-yaml": "^5.2.0"
  }
}
